@inject NavigationManager Navigation
<Router AppAssembly="@typeof(WebApp).Assembly">
    <Found Context="routeData">
        <RouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)" />
    </Found>
    <NotFound>
        @{
            Navigation.NavigateTo("/lock-settings");
        }
    </NotFound>
</Router>